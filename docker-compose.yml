services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: clearboard-frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
    environment:
      - NODE_ENV=development
